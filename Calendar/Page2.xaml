<Page x:Class="Calendar.Page2"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Calendar"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="550"
      Title="Page2">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="7*"/>
            <RowDefinition Height="37*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="dateBlock" Text="Месяц Год" FontSize="28" VerticalAlignment="Center" HorizontalAlignment="Center"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel x:Name="checkBoxes">
                
            </StackPanel>
        </ScrollViewer>

        <Button Grid.Row="2" Content="Сохранить и выйти" FontSize="20" Background="DarkCyan" Click="Button_Click"/>
    </Grid>
</Page>
